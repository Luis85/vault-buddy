{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "editor",
  "description": "The tutorial editor window alone: the editor_* session commands (open a staged capture into a session, read its snapshot, execute a command, close the session, hide the window) that build.rs's app manifest takes out of every other window's reach, plus the file dialogs its save/import flows need. No other capability grants any allow-editor-* permission.",
  "windows": ["editor"],
  "permissions": [
    "allow-editor-open-staged",
    "allow-editor-get-snapshot",
    "allow-editor-execute",
    "allow-editor-close-session",
    "allow-editor-hide-window",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
