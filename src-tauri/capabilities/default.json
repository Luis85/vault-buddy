{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Buddy, panel, bubble, and editor windows: the frontend drives everything through app commands (toggle_panel, close_panel, start_buddy_drag, …); window geometry and visibility are managed Rust-side, so no core:window IPC grants are needed. Region selection runs in the overlay window, which likewise drives everything through app commands.",
  "windows": ["main", "panel", "bubble", "overlay", "editor"],
  "permissions": [
    "core:default",
    "log:default",
    "updater:default",
    "process:allow-restart",
    "dialog:allow-open"
  ]
}
