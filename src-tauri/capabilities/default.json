{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Buddy, panel, and bubble windows: the frontend drives everything through app commands (toggle_panel, close_panel, start_buddy_drag, …); window geometry and visibility are managed Rust-side, so no core:window IPC grants are needed.",
  "windows": ["main", "panel", "bubble"],
  "permissions": [
    "core:default",
    "log:default",
    "updater:default",
    "process:allow-restart",
    "dialog:allow-open"
  ]
}
