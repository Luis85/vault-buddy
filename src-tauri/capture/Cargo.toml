[package]
name = "vault_buddy_capture"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
cpal = "0.18"
mp3lame-encoder = "0.2"
log = "0.4"
vault_buddy_core = { path = "../core" }

[dev-dependencies]
minimp3 = "0.6"
tempfile = "3"
